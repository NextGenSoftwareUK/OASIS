# ─── OASIS Omniverse — doom-browser environment variables ────────────────────
# Copy this file to .env and fill in your values before running npm start.
# Never commit .env to git.

# ── Server port (optional, default: 8765) ─────────────────────────────────────
PORT=8765

# ── Solana RPC (optional, defaults to devnet) ─────────────────────────────────
# Use mainnet-beta for production, devnet for testing
SOLANA_RPC_URL=https://api.devnet.solana.com

# ── MagicBlock SOAR — Gaming Track ────────────────────────────────────────────
# Authority keypair: the wallet that signs score submissions.
# Either a JSON array (e.g. [1,2,3,...]) or a base58 private key string.
# Generate a new one: solana-keygen new --outfile soar-auth.json
# Then fund it on devnet: solana airdrop 1 <pubkey> --url devnet
SOAR_AUTH_KEYPAIR=

# After running `node soar-setup.js`, paste the printed addresses here:
SOAR_GAME_ADDRESS=
SOAR_LEADERBOARD_PDA=

# ── Tapestry — Onchain Social Track ───────────────────────────────────────────
# Get your API key from https://app.usetapestry.dev/
TAPESTRY_API_KEY=

# ── Optional: override the OASIS API base (default auto-detected) ─────────────
# OASIS_API_BASE=https://api.oasisweb4.com
