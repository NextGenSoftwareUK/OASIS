# Pan Galactic Monitor — app

```bash
cd Hitchhikers/Pan_Galactic_Monitor/app
npm install
npm start
```

**URL:** http://localhost:3142  

Edit pins in **`../data/sites.json`**. Optional: `cp .env.example .env` and set `PORT`, `REFRESH_INTERVAL_SEC`, `SITES_JSON_PATH`.
