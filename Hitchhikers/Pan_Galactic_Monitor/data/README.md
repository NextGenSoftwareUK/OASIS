| File | Use |
|------|-----|
| `sites.example.json` | Template; copy to `sites.json` for local edits (or point `SITES_JSON_PATH` at either). |
| `sites.schema.json` | Each item in `sites` array should validate (validate the objects inside `sites`, not the wrapper). |
